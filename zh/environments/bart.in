# Unresolved PyMC-BART extension group for Chapter 7.
# These compatibility pins are separate from pymc.lock.txt so the base PyMC
# environment remains independently installable.
pymc-bart==0.11.0
arviz-stats[xarray]>=0.6.0,<0.7.0
arviz-base>=0.6.0,<0.7.0
